---
const { name, description, image, value } = Astro.props;
---

<div class="p-4 rounded-md border border-[#5C4D3C]">
  <p class="italic font-garamond">"{value}"</p>
  <div class="w-full flex items-center gap-2 ml-auto">
    <img src={image} alt="Testifier" class="w-10 h-10 rounded-full" />
    <div class="flex flex-col text-sm">
      <span class="block mt-2 font-medium">{name}</span>
      <span class="text-[#A89F91]">{description}</span>
    </div>
  </div>
</div>
